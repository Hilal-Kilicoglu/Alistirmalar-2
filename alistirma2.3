#Bu kod, 1'den n'ye kadar olan sayıların çarpımını bulan fonksiyonu içerir.
#n sayısının faktöriyelini bulan fonksiyon.
def faktöriyel(n):
    if n==0:
        return 1
    else:
        return n*faktöriyel(n-1)
