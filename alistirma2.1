#"mastermind" adlı oyun kodu
import random

sayı=str(random.randint(10,98))
while sayı[0]==sayı[1]:
    sayı=random.randint(10,98)
tahmin=0
while (tahmin!=sayı):
    tahmin=str(input("Tahminini gir. Girdiğin sayı 10 ile 98 arasında olmalı ve farklı rakamlardan oluşmalı: "))
    if len(tahmin)==2 and tahmin[0]!=tahmin[1] and tahmin.isdigit()==1:
        doğruyer=0
        yanlışyer=0
        tahmin=str(tahmin)
        sayı=str(sayı)
        if tahmin[0]==sayı[0]:
            doğruyer+=1
        if tahmin[0]==sayı[1]:
            yanlışyer-=1
        if tahmin[1]==sayı[1]:
            doğruyer+=1
        if tahmin[1]==sayı[0]:
            yanlışyer-=1
        if doğruyer==0 and yanlışyer==0:
            print("skor=0")
        if doğruyer==0 and yanlışyer!=0:
            print("Yanlış yer: ", yanlışyer)
        if doğruyer!=0 and yanlışyer==0:
            print("Doğru yer: ", doğruyer)
        if doğruyer==2:
            print("Tebrikler. Doğru tahmin!")
